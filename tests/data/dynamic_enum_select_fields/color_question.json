{
  "definitions": {
    "Color": {
      "title": "Color",
      "type": "string",
      "anyOf": ["awesome_color_options"]
    }
  },
  "title": "Select Color",
  "type": "object",
  "required": ["selectedColor"],
  "properties": {
    "selectedColor": {
      "$ref": "#/definitions/Color",
      "title": "Select color"
    }
  }
}
